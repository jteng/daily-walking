[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "walkwithgod"
version = "0.1.0"
description = "Converted minimal Python project to serve the existing index.html"
authors = [ { name = "(converted)" } ]
